#include "main.h"

/**
 * puts2 - prints one char out of 2 of a string.
 * @str: input string.
 * Return: no return.
 */
void puts2(char *str)
{
	for(int i = 0; i < strlen(input); i+ = 2)
	
	{
		word[i] += input[i];
	}
	_putchar('\n');
}
